<?xml version="1.0" encoding="utf-8"?>
<!--
    package: Once you publish your application, you cannot change
    the package name. The package name defines your application's
    identity, so if you change it, then it is considered to be a
    different application and users of the previous version cannot
    update to the new version. So, it will be "ar.com.promm" for
    ever and ever.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ar.com.promm"
    android:installLocation="preferExternal" >

    <uses-sdk />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Sherlock.Light.NoActionBar" >
        <activity
            android:name=".activities.SplashScreenActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="ar.com.promm.MESSAGE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.FirstTutorialActivity"
            android:label="@string/title_activity_first_tutorial"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".activities.TemporaryLocationActivity"
            android:label="@string/title_activity_temporary_location"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".activities.RegisterStoreActivity"
            android:label="@string/title_activity_register_store"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".activities.OtraTabsActivity"
            android:label="@string/title_activity_tabs"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".activities.AppInfoActivity"
            android:label="@string/title_activity_app_info"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".activities.InfoLocalActivity"
            android:label="@string/title_activity_store_description"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".activities.DetallesPrommActivity"
            android:label="@string/title_activity_detalles_promm"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".activities.DetallesLocalActivity"
            android:label="@string/title_activity_detalles_local"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".activities.GalleryActivity"
            android:label="@string/title_activity_gallery" >
        </activity>

        <!-- Fabric.io meta data -->
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="f36ca6cc40e51ee467ee025c6f25c4dba350fc73" />

    </application>

</manifest>
